{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @orgos/db exec prisma generate && mkdir -p apps/web/node_modules/.prisma/client && cp -r node_modules/.prisma/client/. apps/web/node_modules/.prisma/client/ && pnpm --filter web build",
  "outputDirectory": ".next"
}
